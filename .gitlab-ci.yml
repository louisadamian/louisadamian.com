image: node:14.17.0

#build-job:
#  stage: build
#  script:
#  - yarn install
#  - yarn build
#  only:
#  - master

pages:
  stage: deploy
  cache:
    paths:
    - node_modules/
  script:
  - yarn install
  - yarn build
  - mkdir public
  - mv .vuepress/dist. public  
  artifacts:
    paths:
    - public
  only:
  - master
